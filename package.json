{
  "scripts": {
    "build": "rollup -c",
    "now-build": "npm run build",
    "deploy": "npm run build && now"
  },
  "devDependencies": {
    "now": "^17.1.1",
    "rollup": "^2.3.3"
  }
}
